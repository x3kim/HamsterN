# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# C extensions
*.so
*.pyd

# Distribution / packaging
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
pip-wheel-metadata/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# PyInstaller
#  Usually these files are written by a python script from a template
#  before PyInstaller builds a program, so direct committing them is not advisable.
#  VERSION-INFO.txt will be part  of the executable, it is only needed by PyInstaller
#  *.manifest will be part of the executable, it is only needed by PyInstaller
*.spec
# VERSION-INFO.txt # Entferne das Kommentarzeichen, falls du eine solche Datei manuell verwaltest
# *.manifest       # Entferne das Kommentarzeichen, falls du eine solche Datei manuell verwaltest

# Virtual Environments
.env
.venv
env/
venv/
ENV/
env.bak/
venv.bak/

# VS Code
.vscode/

# Backup files
_old/

# Lokale Highscore-Datei (wenn sie direkt im Projektordner generiert wird und nicht versioniert werden soll)
highscore.txt # Nur hinzufügen, wenn deine Highscore-Datei im Hauptverzeichnis liegt und nicht versioniert werden soll.
                 # Wenn sie im APPDATA-Ordner etc. liegt, ist das hier nicht nötig.